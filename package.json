{
  "name": "loom",
  "version": "0.1.0",
  "description": "Multi-agent deliberation protocol for opencode — a knitting machine for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "skills",
    "commands"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "npx tsx test/engine.test.ts && npx tsx test/tiers.test.ts && npx tsx test/composer.test.ts && npx tsx test/shared-files.test.ts && npx tsx test/validation.test.ts && npx tsx test/integration.test.ts",
    "install:plugin": "node scripts/install.mjs",
    "update:plugin": "node scripts/update.mjs"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "@opencode-ai/sdk": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.14",
    "@opencode-ai/sdk": "^1.18.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  }
}
