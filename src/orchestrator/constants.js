// Re-exported for backward compat — canonical source is now ../orchestrator.js (Phase 3 collapse)
// Kept to avoid breaking imports from orchestrator/*.js; do not add new constants here.
export { SUMMARY_TRUNCATE_LEN, MAX_ORCHESTRATOR_MESSAGES } from "../orchestrator.js";
